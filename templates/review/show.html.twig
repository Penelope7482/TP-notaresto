{% extends 'base.html.twig' %}

{% block title %}Commentaires
{% endblock %}

{% block body %}
	<h1>Commentaires</h1>

	<div class="card">
		<div class="card-header bg-dark ">
			<div class="card-title text-white">
				Commentaire numéro:
				{{ review.id }}
			</div>
			<div class="card-text text-white">Auteur:
				{{review.user.firstname}}</div>
			<div class="card-text text-white">Date:
				{{ review.createdAt ? review.createdAt|date('d-m-Y H:i:s') : '' }}</div>
		</div>
		<div class="card-body">
			<div class="card-title">Message:
			</div>
			<div class="card-text">
				{{ review.message }}</div><br>
			<div class="card-subtitle">Note:
				{{ review.rating }}
			</div><br>
			<div class="card-footer">
				<a href="{{ path('review_index') }}">back to list</a>

				<a href="{{ path('review_edit', {'id': review.id}) }}">edit</a>

				{{ include('review/_delete_form.html.twig') }}
			</div>

		</div>


	{% endblock %}
