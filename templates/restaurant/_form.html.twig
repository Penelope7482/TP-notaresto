{{ form_start(form) }}
<div class="mt-3 mx-auto col-sm-6 ">
		<div class="card px-4 py-4 text-white bg-dark">
     {{ form_widget(form) }}  
    <button class="btn btn-info">{{ button_label|default('Enregistrer') }}</button>
    </div>
    </div>
{{ form_end(form) }}
